spring:
  application:
    name: vu-app
  cloud:
    config:
      uri: http://config-server:8888/
management:
  endpoints:
    web:
      exposure:
        include: "*"


---
spring:
  profiles: dev
  application:
    name: vu-app
  cloud:
    config:
      uri: http://localhost:8888/
